import { MapStyled } from "./style";

const MoleculeMap = ({ src, width, height }) => {
  return (
    <>
      <MapStyled src={src} width={width} height={height}>

      </MapStyled>
    </>
  );
};

export default MoleculeMap;
